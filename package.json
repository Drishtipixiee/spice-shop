{
  "name": "spice-shop",
  "version": "1.0.0",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r admin dist/ && cp -r public dist/ && cp -r data dist/ && cp -r api dist/ && cp index.html dist/ && cp login.html dist/ && cp delivery-map.html dist/ && cp 404.html dist/ && cp sw.js dist/ && cp manifest.json dist/"
  },
  "dependencies": {}
}
