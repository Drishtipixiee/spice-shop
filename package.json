{
  "dependencies": {
    "pg": "^8.22.0"
  },
  "scripts": {
    "build": "node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.cpSync('frontend', 'dist', { recursive: true })\""
  },
  "devDependencies": {
    "vercel": "^54.17.1"
  }
}
