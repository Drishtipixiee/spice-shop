[build]
  base    = "frontend"
  command = "npm install && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# No NEXT_PUBLIC_API_URL set → frontend uses its own /api routes from data.json
# This means ZERO backend dependency — site works fully on Netlify alone!
